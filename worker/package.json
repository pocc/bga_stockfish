{
  "name": "stockfish-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:glue": "node scripts/build-glue.mjs",
    "predeploy": "npm run build:glue && npm run test:run",
    "predev": "npm run build:glue",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251101.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.63.0"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.4",
    "chess.js": "^1.4.0",
    "js-chess-engine": "^2.4.6"
  }
}
